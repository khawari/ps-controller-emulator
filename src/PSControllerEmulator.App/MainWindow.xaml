<Window x:Class="PSControllerEmulator.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PS Controller Emulator"
        Height="560"
        Width="900"
        MinHeight="460"
        MinWidth="760"
        Background="#F6F8FA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <Button x:Name="RefreshButton"
                    DockPanel.Dock="Right"
                    Width="108"
                    Height="36"
                    Click="RefreshButton_Click"
                    Content="Refresh" />
            <StackPanel>
                <TextBlock Text="PS Controller Emulator"
                           FontSize="26"
                           FontWeight="SemiBold"
                           Foreground="#111827" />
                <TextBlock x:Name="StatusText"
                           Margin="0,6,0,0"
                           FontSize="13"
                           Foreground="#4B5563"
                           Text="Starting controller scan..." />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1"
                Margin="0,22,0,18"
                Padding="14,10"
                Background="#FFFFFF"
                BorderBrush="#D7DEE8"
                BorderThickness="1"
                CornerRadius="8">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           FontSize="13"
                           Foreground="#374151"
                           Text="Discovery mode" />
                <TextBlock x:Name="ModeBadge"
                           DockPanel.Dock="Right"
                           Padding="12,4"
                           VerticalAlignment="Center"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="#0F5132"
                           Background="#D1E7DD"
                           Text="Mock fallback" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <Border Background="#FFFFFF"
                    BorderBrush="#D7DEE8"
                    BorderThickness="1"
                    CornerRadius="8">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ControllerList" Margin="18">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,12"
                                        Padding="16"
                                        Background="#FAFBFC"
                                        BorderBrush="#E5E7EB"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   FontSize="18"
                                                   FontWeight="SemiBold"
                                                   Foreground="#111827"
                                                   Text="{Binding DisplayName}" />
                                        <TextBlock Grid.Column="1"
                                                   FontSize="13"
                                                   Foreground="#374151"
                                                   Text="{Binding Connection}" />
                                        <TextBlock Grid.Column="2"
                                                   HorizontalAlignment="Right"
                                                   FontSize="13"
                                                   Foreground="#374151"
                                                   Text="{Binding Battery}" />

                                        <TextBlock Grid.Row="1"
                                                   Grid.ColumnSpan="3"
                                                   Margin="0,10,0,0"
                                                   FontSize="13"
                                                   Foreground="#4B5563"
                                                   Text="{Binding InputState}" />

                                        <TextBlock Grid.Row="2"
                                                   Grid.ColumnSpan="3"
                                                   Margin="0,8,0,0"
                                                   FontSize="12"
                                                   Foreground="#6B7280"
                                                   Text="{Binding Features}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <TextBlock x:Name="EmptyState"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#6B7280"
                       Text="No controllers detected" />
        </Grid>
    </Grid>
</Window>
